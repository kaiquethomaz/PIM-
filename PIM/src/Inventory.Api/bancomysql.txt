CREATE DATABASE inventory;
USE inventory;

-- Tabela de migrations (opcional)
CREATE TABLE __EFMigrationsHistory (
    MigrationId VARCHAR(150) NOT NULL,
    ProductVersion VARCHAR(50) NOT NULL,
    PRIMARY KEY (MigrationId)
);

CREATE TABLE categorias (
    Id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(255) NOT NULL UNIQUE
);

CREATE TABLE fornecedores (
    Id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(255) NOT NULL,
    contato VARCHAR(50) NOT NULL
);

CREATE TABLE usuarios (
    Id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(255) NOT NULL,
    email VARCHAR(255) NOT NULL UNIQUE,
    senha VARCHAR(255) NOT NULL,
    tipo VARCHAR(50) NOT NULL
);

CREATE TABLE produtos (
    Id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(255) NOT NULL,
    categoria_id INT NOT NULL,
    fornecedor_id INT NOT NULL,
    preco DECIMAL(10,2) NOT NULL,
    quantidade INT NOT NULL,
    FOREIGN KEY (categoria_id) REFERENCES categorias(Id),
    FOREIGN KEY (fornecedor_id) REFERENCES fornecedores(Id)
);

CREATE TABLE movimentacoes (
    Id INT AUTO_INCREMENT PRIMARY KEY,
    produto_id INT NOT NULL,
    tipo VARCHAR(20) NOT NULL,
    quantidade INT NOT NULL,
    data DATETIME NOT NULL,
    usuario_id INT NOT NULL,
    FOREIGN KEY (produto_id) REFERENCES produtos(Id),
    FOREIGN KEY (usuario_id) REFERENCES usuarios(Id)
);

-- INSERTS

INSERT INTO __EFMigrationsHistory VALUES ('20260504211357_InitialCreate','8.0.0');

INSERT INTO categorias VALUES
(1,'Periféricos'),
(2,'Setup Gamer'),
(3,'Mobilidade Tech');

INSERT INTO fornecedores VALUES
(1,'Fornecedor Tech','11999999999'),
(2,'NebulaTech Solutions','11966666666'),
(3,'Quantum Devices Co.','11955555555');

INSERT INTO usuarios VALUES
(1,'Administrador','admin@pim.local','$2a$11$Lvk8AQI7mb20VbLSsjYWQ.PIl9IFkeSNJLdsHXeVCFNoHVi7aS8ym','Admin');

INSERT INTO produtos VALUES
(1,'Mouse Gamer',1,1,150.00,27),
(3,'Monitor Ultrawide 29"',2,2,1200.00,0),
(4,'Teclado Mecânico RGB Pro',2,3,350.00,0),
(5,'Power Bank 20000mAh Turbo',3,2,180.00,10),
(6,'Fone Bluetooth Noise Canceling',3,3,400.00,15),
(8,'Fone Bluetooth Noise Canceling',3,3,400.00,0);

INSERT INTO movimentacoes VALUES
(1,1,'Exit',3,'2026-05-04 21:45:32',1),
(2,1,'Entry',20,'2026-05-04 21:49:11',1),
(3,5,'Entry',10,'2026-05-04 21:54:54',1),
(4,6,'Entry',15,'2026-05-04 21:55:00',1);